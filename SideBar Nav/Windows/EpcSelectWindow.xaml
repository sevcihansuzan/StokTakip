<Window x:Class="SideBar_Nav.Windows.EpcSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        BorderBrush="#FFB197FC" BorderThickness="1" 
        Title="EPC Listesi" Height="400" Width="300" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="5" Grid.Row="0">
            <CheckBox x:Name="chkLiveMode" Content="Canlı EPC okuma" IsChecked="True" Margin="0,0,10,0" ToolTip="Anlık okuman EPC'leri listeler. Kapalı durumdayken EPC bir kere bile okunsa listede tutmaya devam eder."/>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="dataGridEpc" AutoGenerateColumns="False" IsReadOnly="True"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="EPC" Binding="{Binding}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Row="2" x:Name="btnEPCsec"
        Content="EPC seç"
        Margin="5"
        Padding="14,6"
        Click="btnEPCsec_Click"/>
        <TextBlock Grid.Row="3" Text="Not: Çift tıklayarak da EPC seçebilirsiniz." Margin="5"
            Foreground="#6b7280"/>
    </Grid>
</Window>
